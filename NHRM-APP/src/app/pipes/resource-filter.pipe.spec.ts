import { ResourceFilterPipe } from './resource-filter.pipe';

describe('ResourceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
